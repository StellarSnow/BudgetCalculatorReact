class Transaction {
  constructor(amount, description, date, id) {
    this.amount = amount;
    this.description = description;
    this.date = date;
    this.id = id;
  }
}

export default Transaction;